# قوانین

قبل از هر تغییر این فایل را بخوان.

## کامنت فایل

بالای هر فایل `.c` / `.h`:
- لحن مهندسی، نه آموزشی
- انگلیسی و فارسی

## کامنت تابع

بالای هر تابع:
- کار تابع چیست (انگلیسی و فارسی)
- هر پارامتر یعنی چی، واحدش چیست، محدوده‌اش چیست (مثلاً mV، ms، درصد)
- مقدار برگشتی چیست
- مثال: `@param u32_batteryMv [EN] Battery voltage in mV, 21V=0% 28V=100% / ولتاژ باتری`
- انگلیسی و فارسی اجباری

## هماهنگی

قبل از **هر** تغییر (کد، README، AI، پوشه):
1. بگو کدام فایل‌ها عوض می‌شوند
2. بگو چرا
3. صبر کن تا کاربر تأیید کند
4. بعد انجام بده

بدون تأیید، فایل را عوض نکن.

## مالکیت اسناد

- `AI_AGENT_RULES.md` تنها منبع قوانین AI و Agentها است.
- `AI_EXECUTION_REPORT.md` تنها گزارش اجرای تغییرات سراسری است.
- `README.md` ریشه صفحهٔ معرفی و درخت اتصال کل پروژه است؛ فقط عامل مسئول تغییرات سراسری و با تأیید کاربر آن را اصلاح می‌کند.
- Agentی که فقط روی یک ماژول کار می‌کند نباید قوانین، گزارش AI یا README ریشه را برای تغییرات محلی ماژول تغییر دهد.
- اگر تغییر ساختار یا اتصال کل پروژه روی اسناد سراسری اثر دارد، تغییر باید جداگانه اعلام و تأیید شود.

## MISRA C

همهٔ کد روی MISRA C نوشته شود.

## سادگی و خوانایی توابع

توابع را ساده و خوانا بنویس تا بشه بعدا اصلاحشون کرد — صرفاً خطی ننویس چون از خوانایی می‌اندازد.
- هر تابع یک کار کند (Single Responsibility)
- از ifهای تودرتوی عمیق پرهیز کن، ولی گام‌ها را با نام متغیر واضح و کامنت جدا کن تا خوانا بماند، نه فقط پشت‌سرهم on/delay/off
- تابع بازر جدا باشد و از هر سناریو قابل صدا زدن باشد
- بالای هر تابع حتماً توضیح بده چیکار می‌کنه و هر پارامتر یعنی چی (واحد، محدوده)

## فرمول‌ها خطی نباشد

فرمول‌ها را خطی ننویس — فرمول خطی خوانایی را از بین می‌برد.

- هر فرمول را به گام‌های کوچک با متغیرهای معنادار خورد کن، نه یک خط طولانی
- مثال بد (خطی):

```c
uint8_t__pct = (uint8_t)((offset * 100u) / range);
```

- مثال خوب (غیرخطی، خوانا):

```c
uint32_t uint32_t__voltageRangeMv = UI_BAT_V_MAX_MV - UI_BAT_V_MIN_MV;
uint32_t uint32_t__voltageOffsetMv = batteryMv - UI_BAT_V_MIN_MV;
uint32_t uint32_t__scaledOffset = uint32_t__voltageOffsetMv * 100u;
uint8_t uint8_t__batteryPercent = (uint8_t)(uint32_t__scaledOffset / uint32_t__voltageRangeMv);
```

- برای چشمک: اول درصد باقی‌مانده، بعد دوره تقسیم بر ۱۰۰، بعد ضرب، بعد کف، بعد محاسبه on/off — هر گام یک متغیر با نام مرتبط
- این قانون برای همه فرمول‌ها (باتری به درصد، چشمک زرد، سبز، محاسبه بوق) اجباری است

## جداسازی توابع با علامت مشخص

کل توابع را با علامت مشخص از هم جدا کن که اول و آخرشان مشخص باشد - بالای هر تابع این مدلی جدا بشه.

- از جداکننده‌های یکسان استفاده کن: `/* ==================== Blink / Poll timings ==================== */` یا `/* ==================== Green LED ==================== */` یا `/* ==================== Buzzer / Beep ==================== */`
- بالای هر تابع (هم در `.h` و هم در `.c`) این جدا کننده باشد تا اسکرول سریع و خوانایی بالا برود
- مثال درخواستی کاربر:

```c
/* ==================== Blink / Poll timings ==================== */
#define UI_BLINK_PERIOD_MS 1000u

/* ==================== Green LED ==================== */
static void func__green(bool bool__greenOn) { ... }

/* ==================== Red LED ==================== */
static void func__red(bool bool__redOn) { ... }

/* ==================== Buzzer / Beep ==================== */
int32_t func__Ui_Buzzer_Tick(uint32_t periodMs, uint8_t dutyPercent, uint8_t beepCount, uint32_t gapMs);

/* ==================== Ui Init ==================== */
void func__Ui_Init(void);
```

- در همه فایل‌های `.c` و `.h` این قانون رعایت شود: بالای هر تابع (و هر گروه define) یک `/* ==================== Name ==================== */`
- برای UI الان دو فایل جدا `ui_led.h/c` و `ui_buzzer.h/c` هر دو همین قانون را دارند

## جداسازی بازر از LED در کد

بازر را از LED در کد جدا کن - الان دو فایل جدا در همین پوشه UI.

- قبلاً تمام کدها و توابع مربوط به بازر با منطق LED قاطی بودند.
- الان طبق درخواست کاربر UI دو بخش مستقل دارد: LED و BUZZER در همین پوشه UI.
  - `ui_led.h` / `ui_led.c` : LED شامل `green/red/yellow/all_off`, `BatteryVoltageToPercent`, `ScenarioInputOk`, `Charging_Tick`, `BatteryRun_Tick`, `InputOverVoltage_Tick`, `Tick`, `Init`, `BoardTest`؛ هر تابع با `/* ==================== */` جدا. سناریوهای صریح `BoardTest`، `BatteryRun` و `InputOverVoltage` سرویس بوق را صدا می‌زنند و `InputOk`/`Charging` آن را خاموش می‌کنند.
  - `ui_buzzer.h` / `ui_buzzer.c` : فقط یک API عمومی به نام `func__Ui_Buzzer_Tick(periodMs, dutyPercent, beepCount, gapMs)`؛ الگو را بدون قفل کردن تسک اجرا می‌کند و در حالت معتبر زمان مراجعه بعدی RTOS را برمی‌گرداند؛ صفر خاموشی معتبر و منفی یک خطا است.
  - محدودیت‌های خود سرویس بوق در `ui_buzzer.h` هستند؛ سیاست سناریویی بوق، آستانه‌های BatteryRun، دوره‌ها، dutyها، تعداد پالس و گپ در `ui_led.h` هستند چون مالک منطق سناریو است. وضعیت داخلی در `ui_buzzer.c` است و تابع داخلی جدا برای کنترل بوق وجود ندارد.
- داخل هر بخش، توابع مربوط به همان کار باشد، نه قاطی.
- مثال ساختار فعلی:

```c
/* ui_led.h / ui_led.c */
/* ==================== Battery Voltage To Percent ==================== */
/* ==================== Green LED ==================== */
/* ==================== Scenario InputOk ==================== */
/* ==================== Scenario Charging Tick ==================== */
/* ==================== Scenario BatteryRun Tick ==================== */
/* ==================== Scenario Input Overvoltage ==================== */

/* ui_buzzer.h / ui_buzzer.c */
/* ==================== Buzzer / Beep ==================== */
/* ==================== Buzzer service ==================== */
int32_t func__Ui_Buzzer_Tick(uint32_t periodMs,
                             uint8_t dutyPercent,
                             uint8_t beepCount,
                             uint32_t gapMs);
```

- این جداسازی (دو فایل LED و BUZZER) تا آخر پروژه رعایت شود و در AI هم ثبت است

## وضعیت فعلی اتصال Measurement به UI

- ADC1 و DMA فعال هستند و Measurement فریم‌های ADC را هر `10ms` تبدیل می‌کند.
- در مرحلهٔ فعلی فقط `UINT32_T__G__MeasInputVoltageMv` به Task UI وصل است.
- تا زمان تأیید مرحلهٔ اندازه‌گیری باتری، `UINT32_T__G__BatteryVoltageMv` در `task_ui.c` ورودی تست دستی Live Expressions باقی می‌ماند.
- اگر اولین فریم ADC معتبر نباشد، ورودی برای UI برابر صفر و از نظر سناریو قطع در نظر گرفته می‌شود؛ این حالت امن است.
- اتصال ورودی باعث تأخیر جدید یا قفل‌شدن CPU نمی‌شود؛ ADC و DMA سخت‌افزاری هستند و UI فقط یک مقدار ۳۲ بیتی را می‌خواند.
- ماژول‌های فقط‌ماژول نباید این مرحلهٔ اتصال سراسری را با تغییر README ریشه یا گزارش AI مستندسازی کنند؛ مسئول شاخه این اسناد را به‌روزرسانی می‌کند.

## وضعیت فعلی سناریوهای UI

این بخش وضعیت مورد تأیید فعلی UI را ثبت می‌کند تا قوانین عمومی با رفتار سناریوها تناقض نداشته باشند:

- تشخیص اتصال ورودی با هیسترزیس انجام می‌شود: ورودی `>=21V` وصل، ورودی `<=20V` قطع، و بازهٔ ۲۰ تا ۲۱ ولت وضعیت قبلی را حفظ می‌کند.
- خطای `InputOverVoltage` فقط با ولتاژ ورودی فعال می‌شود: ورودی `>28V` خطا را فعال و ورودی `<=27V` آن را پاک می‌کند؛ بازهٔ ۲۷ تا ۲۸ ولت وضعیت خطا را حفظ می‌کند.
- `InputOk`: ورودی وصل و باتری `>=28V`؛ سبز دائم و زرد/قرمز/بوق خاموش.
- `Charging`: ورودی وصل و باتری `<28V`؛ سبز دائم، زرد متناسب با ماندهٔ شارژ چشمک‌زن و قرمز/بوق خاموش.
- `BatteryRun`: ورودی قطع؛ سبز بر اساس درصد خطی باتری بین ۲۱ تا ۲۸ ولت چشمک‌زن و زرد/قرمز خاموش.
- بوق BatteryRun در چهار بازه اجرا می‌شود: زیر ۴۰٪ یک بوق هر ۶۰ ثانیه، زیر ۲۰٪ دو بوق هر ۶۰ ثانیه، زیر ۱۰٪ سه بوق هر ۲۰ ثانیه، و زیر ۱٪ یک بوق ممتد ۱۰ ثانیه‌ای و سپس خاموشی همهٔ خروجی‌ها تا بازیابی باتری.
- ثابت‌های سیاست سناریو در `ui_led.h`، محدودیت‌های عمومی سرویس در `ui_buzzer.h`، و وضعیت زمان‌بندی داخلی در فایل‌های `.c` همان بخش نگهداری می‌شوند.

## برگه تست و اعتبارسنجی عملی روی برد

برای ولیدیشن رفتار واقعی UI روی برد، فایل اصلی ثبت تست این است:

`Firmware/Modules/Ui/UI_Board_Validation.xlsx`

- این فایل شامل پنج برگهٔ کاری است: راهنما، برنامهٔ تست، مراجع سناریو، ثبت ایراد و تأیید نهایی؛ برگهٔ فهرست‌های داخلی فقط برای فهرست‌های انتخابی Excel است.
- دامنهٔ این فرم فقط رفتار LED و BUZZER است؛ فعال‌بودن ADC/Measurement به‌معنی پوشش تست ADC در این فرم نیست.
- در مرحلهٔ فعلی ورودی UI از دو منبع می‌آید:
  - `UINT32_T__G__MeasInputVoltageMv` بر حسب mV، از Measurement/ADC و فقط پس از معتبرشدن فریم
  - `UINT32_T__G__BatteryVoltageMv` بر حسب mV، ورودی تست دستی Live Expressions تا مرحلهٔ باتری
- قبل از شروع، commit firmware، شناسهٔ برد، تست‌کننده، ابزار اندازه‌گیری و تاریخ باید در برگهٔ «تأیید نهایی» ثبت شوند.
- هر ردیف «برنامهٔ تست» باید با نتیجهٔ واقعی/اندازه‌گیری، وضعیت (`در انتظار`، `قبول`، `رد`، `مسدود` یا `نیاز به تکرار`)، تست‌کننده و تاریخ تکمیل شود.
- هر تفاوت بین انتظار و واقعیت باید با شناسهٔ یکتا در ستون شرح ایراد و برگهٔ «ثبت ایراد» ثبت شود؛ پس از اصلاح، تست مربوط باید دوباره اجرا و وضعیت ایراد بسته شود.
- مدت و دورهٔ بوق‌ها برای کوتاه‌کردن تست تغییر داده نشود؛ تست‌های ۲۰ و ۶۰ ثانیه‌ای باید با دورهٔ واقعی سناریو بررسی شوند.
- قطبیت پایه‌های `PA4`، `PB0`، `PB1` و `PB10` تا اندازه‌گیری روی برد، شماتیکی محسوب می‌شود و نتیجهٔ اندازه‌گیری باید در تست پایه‌ها ثبت شود.
- تأیید نهایی فقط زمانی مجاز است که همهٔ تست‌های لازم `قبول` باشند و هیچ ایراد `باز` یا `در حال بررسی` باقی نمانده باشد.
- بعد از هر نوبت تست واقعی یا تغییر سناریوی مورد تأیید، همین فایل Excel باید به‌روز شود؛ مستندات به‌تنهایی جایگزین مشاهدهٔ روی برد نیستند.

## RTOS ساده و خوانا - بدون قفل میکرو

برنامه کاملاً به شکل RTOS نوشته شود ولی ساده و خوانا بماند، نه شلوغ.

- از `HAL_Delay` اصلاً استفاده نکن (میکرو را قفل می‌کند، همه تسک‌ها می‌ایستند)
- در RTOS از `vTaskDelay` یا `vTaskDelayUntil` استفاده کن — این‌ها میکرو را قفل نمی‌کنند، فقط همان تسک می‌خوابد و بقیه تسک‌ها (measurement, protection, control) اجرا می‌شوند
- پس `vTaskDelay` داخل تسک‌ها مجاز است و ساده و خواناست، ولی داخل توابع ماژول که منطق طولانی دارند، بهتر است خورد شود یا با `vTaskDelayUntil` دوره‌ای شود
- کارهای طولانی را خورد کن تا میکرو قفل نکند: هر تسک هر بار یک کار کوچک انجام دهد و با `vTaskDelay(10ms)` یا `vTaskDelayUntil` برگردد
- الگوی ساده RTOS و خوانا:

```c
void func__TaskUi(void *arg) {
  TickType_t lastWake = xTaskGetTickCount();
  for (;;) {
    vTaskDelayUntil(&lastWake, pdMS_TO_TICKS(10));
    func__Ui_Tick(inputMv, batteryMv); // یک تیکه کوچک، بدون delay داخلش
  }
}
```

یا اگر خواستی داخل سناریو delay داشته باشی، چون RTOS است و فقط همین تسک می‌خوابد، مجاز و خواناست:

```c
void func__Ui_ScenarioInputOk(void) {
  green(true); red(false); yellow(false);
  vTaskDelay(pdMS_TO_TICKS(500)); // فقط Ui می‌خوابد، بقیه تسک‌ها کار می‌کنند
}
```

- مهم: `HAL_Delay` ممنوع، `vTaskDelay` در تسک مجاز و ساده است. کد را شلوغ نکن با استیت‌ماشین‌های خیلی پیچیده اگر با یک `vTaskDelay` ساده هم میکرو قفل نمی‌شود
- تا آخر پروژه RTOS ساده و خوانا بماند، نه شلوغ

## فایل ui_config.h حذف شد و UI دو بخش شد

فایل `ui_config.h` اضافی بود و فایل‌های تک‌فایلی قدیمی UI نیز حذف شدند؛ UI اکنون به دو بخش LED و BUZZER تقسیم شده است.

- قبلاً تنظیمات UI در یک هدر/فایل عمومی و منطق LED و بوق کنار هم بود.
- الان طبق درخواست کاربر ثابت‌ها در هدرهای خودشان هستند:
  - `ui_led.h` : ثابت‌های پیش‌فرض LED و سیاست سناریوها شامل BAT_V_MIN/MAX، آستانه‌های اتصال و هیسترزیس ورودی، آستانه و زمان‌بندی InputOverVoltage، دوره‌های چشمک، درصدها، ثابت‌های BatteryRun و `TICK_MS`
  - `ui_buzzer.h` : ثابت‌های عمومی سرویس بوق شامل BUZZER_PERCENT_SCALE، BUZZER_DUTY_MAX_PERCENT، MIN_PERIOD_MS، MIN_GAP_MS، CHECK_PERCENT، کدهای بازگشتی و مقدار BoardTest
- `app_config.c` پیش‌فرض‌های زمان‌بندی عمومی LED و نگاشت باتری را از هدرهای UI می‌گیرد؛ سیاست BatteryRun مستقیماً از ثابت‌های سناریویی `ui_led.h` استفاده می‌کند.
- `ui_buzzer.c` فقط سرویس `func__Ui_Buzzer_Tick` را پیاده می‌کند؛ سناریوها از طریق همین API و بدون نوشتن مستقیم PA4 بوق را کنترل می‌کنند.
- اگر ثابت محدودیت عمومی سرویس بوق اضافه شد در `ui_buzzer.h` بگذار؛ اگر ثابت سیاست یک سناریوی UI اضافه شد در `ui_led.h` بگذار.

## نام‌گذاری متغیر

- اول تایپ کامل بعد نام متغیر: تایپ کامل مثل `uint8_t`, `uint16_t`, `uint32_t`, `int32_t`, `bool`, `float`
- بعد از تایپ **دو تا `_` پشت‌سرهم `__`** برای تفکیک بهتر و قابل تعریف بودن: `__` بجای یکی
- اگر گلوبال بود تایپ با حروف بزرگ و با `__G__` برای گلوبال: مثلاً `UINT32_T__G__InputVoltageMv`, `UINT8_T__G__BatteryPercent`, `BOOL__G__InputPresent`
- اگر داخلی (لوکال) بود تایپ با حروف کوچک و `__`: مثلاً `uint32_t__inputVoltageMv`, `uint8_t__batteryPercent`, `bool__inputPresent`
- برای استاتیک سطح فایل هم قانون گلوبال (حروف بزرگ) اعمال می‌شود: `BOOL__G__BuzzerPatternValid`, `UINT32_T__G__BuzzerPeriodMs`
- **نام باید مرتبط با کاری باشد که برایش نوشته شده**: نام متغیر باید بگوید چه کاری می‌کند، نه نام عمومی مثل `tmp`, `data`, `val`
  - خوب: `uint32_t__batteryVoltageMv`, `uint8_t__batteryPercent`, `bool__inputPresent`, `uint32_t__greenBlinkOnMs`, `uint32_t__dutyWindowMs`
  - بد: `uint32_t__x`, `uint32_t__temp`, `uint8_t__val`
- ثابت‌های `#define` با پیشوند ماژول (مثل `UI_`) در هدر همان بخش (`ui_led.h` یا `ui_buzzer.h`) باشند.

## نام‌گذاری ثابت

- هر ثابت `#define` باید با نام ماژول/فایل شروع شود تا معلوم باشد کجا تعریف شده
- مثال: `UI_SELFTEST_LED_MS` یعنی ثابت ماژول UI، در `ui_led.h` تعریف شده است
- **آیا `UI.c_UI_SELFTEST_LED_MS` شدنی است؟**
  - در C نام ماکرو فقط حروف، عدد و `_` می‌تواند داشته باشد، نقطه `.` غیرمجاز است، پس `UI.c_...` کامپایل نمی‌شود
  - اگر بخواهی نام فایل را دقیق بیاوری باید با `_` بنویسی؛ اما پیشوند `UI_` برای این ماژول کافی است
  - ثابت‌های LED در `ui_led.h` و ثابت‌های سرویس بوق در `ui_buzzer.h` قرار می‌گیرند
  - برای ماژول‌های دیگر: `TASK_STACK_UI`, `TASK_PRIO_UI` در `rtos_config.h` هستند (پیشوند `TASK_` + نام ماژول)، `PIN_LED_G_PORT` در `board_pins.h` (پیشوند `PIN_`)
  - پس قانون فعلی: ثابت‌ها با پیشوند ماژول شروع شوند (`UI_`, `TASK_`, `PIN_`, `MODULE_`, `APP_`) که خودش نام فایل/ماژول را می‌رساند
- همه ثابت‌های جدید باید همین قانون را رعایت کنند و مرتبط با کارشان نام‌گذاری شوند

## نام‌گذاری تابع

- فقط توابعی که خودمان می‌نویسیم (Firmware/App, Bsp, Modules, Rtos, Config) باید اولشان کلمه `func__` با **دو آندرلاین `__`** اضافه شود (بجای یکی)
- به توابع سیستمی (HAL, FreeRTOS, CMSIS) کاری نداشته باش
- مثال: `func__Ui_Init`, `func__Ui_BoardTest`, `func__Ui_Buzzer_Tick`, `func__BspGpio_Write`, `func__App_Start`, `func__TaskUi`
- برای `static` داخلی هم همین قانون: `func__green`, `func__all_off` در صورت نیاز؛ سرویس بوق فعلی تابع داخلی جدا ندارد.
- در `.h` و `.c` و جاهایی که صدا زده می‌شود همه باید با `func__` بیاید
- **نام فایل اولش نیاید**، فقط بعد از تایپ `__` باشد (مثلاً `uint32_t__` و `func__`)

## مدیریت حافظه

مدیریت حافظه را در تمام کارها در نظر داشته باش تا آخر پروژه هم این کار را باید انجام بدی.

- در این پروژه فقط تخصیص استاتیک استفاده شود؛ Dynamic allocation در `FreeRTOSConfig.h` خاموش بماند.
  - تسک‌ها با `xTaskCreateStatic` ساخته شوند (نه `xTaskCreate` که تخصیص پویا می‌کند)
  - بافرهای Idle/Timer در `freertos_hooks.c` استاتیک باشند (`s_idle_tcb`, `s_idle_stack`)
  - `heap_4.c` ممکن است به‌عنوان فایل Vendor در مخزن باقی بماند، اما نباید در Build لینک شود یا استفاده شود.
- اندازه استک تسک‌ها در `rtos_config.h` با واحد **word** (نه بایت) تعریف شود و در `README` هر ماژول ذکر شود
- از آرایه‌های بزرگ روی استک پرهیز کن؛ اگر لازم بود `static` یا `const` بگذار یا سایز را در `rtos_config.h` زیاد کن
- `APP_CONFIG` و همه جدول‌های ثابت باید `const` باشند تا در Flash بمانند نه RAM
- `vApplicationStackOverflowHook` باید فعال باشد و در سرریز، اینتراپت‌ها را خاموش و در حلقه بی‌نهایت بماند
- بعد از هر اضافه کردن پریفرال یا ماژول، RAM/Flash مصرفی را چک کن (Map file) و در README بنویس
- تا آخر پروژه، هر تغییر باید از نظر حافظه بررسی شود (استک، هیپ، بافر DMA، تعداد تسک)

## آموزش

همراه هر کد، دلیل و آموزش هم داده شود تا معلوم باشد چرا این‌طور نوشته شده.

## پوشه CubeMX و CubeIDE

این دو را با `Firmware` قاطی نکن.

| پوشه | چیست |
|---|---|
| `CubeMX/` | فایل `.ioc` (تنظیمات مکعب) |
| `CubeIDE/` | پروژه STM32CubeIDE بعد از Generate (`Core`, `Drivers`, `.project`) |
| `Firmware/` | کد محصول. Generate اینجا نرود |

CubeMX جدا باز می‌شود (IDE ۲.۱ از File→New پروژه STM32 نمی‌سازد).

Project Manager:
- Name: `CubeIDE`
- Location: ریشهٔ ریپو `ChangeOver` (نتیجه: `ChangeOver/CubeIDE/`)
- بعد از Generate یک کپی `.ioc` در `CubeMX/` بگذار تا جای فایل مکعب گم نشود

Workspace در CubeIDE: ریشهٔ `ChangeOver`، نه داخل `CubeIDE`.

## README هر ماژول

هر ماژول یک `README.md` داخل **همان پوشه** دارد؛ README ماژول را جای دیگری نساز و README ماژول دیگری را تغییر نده.

Agent مسئول همان ماژول موظف است README خودش را هنگام ایجاد یا تغییر رفتار، فایل‌ها، پایه‌ها، تست یا اتصال ماژول تکمیل و به‌روز کند. Agent سراسری فقط در صورت اثر مستقیم یک تغییر کل پروژه و با تأیید کاربر به آن README دست می‌زند؛ تغییر دستی و غیرضروری READMEهای ماژول ممنوع است.

README ماژول فقط مستندات همان ماژول را نگه می‌دارد. افزودن خط در بخش **تاریخچه** اجباری نیست و فقط با درخواست صریح کاربر انجام می‌شود.

قالب اجباری `README.md` ماژول:

1. **وضعیت** — فعال یا اسکلت؛ مقدار `MODULE_*` در `modules_enable.h`
2. **تاریخچه** — در صورت درخواست کاربر، تاریخ + چه عوض شد (از جدید به قدیم)
3. **فایل‌ها** — `.c` / `.h` همین پوشه و تسک مربوط در `Rtos`
4. **توابع** — نام دقیق + کار (عمومی، `static` داخلی، تسک)
5. **پایه‌ها** — شماره (مثل PB0) + لیبل Cube/شماتیک + نقش + HIGH یعنی چه
6. **پیش‌فرض امن** — بعد از `Init` خروجی‌ها چه وضعی دارند
7. **درخت اتصال** — این ماژول به کدام فایل‌ها وصل است (صدا می‌زند / صدا زده می‌شود)

اگر ماژول پایهٔ GPIO ندارد، در بخش پایه‌ها صریح بنویس: پایه ندارد.

قطبیت تا روی برد اندازه نشده «شماتیک» است، نه اندازه‌گیری‌شده.

ADC / PWM / UART / رله را در برگهٔ ماژول خاموش، Enable نکن مگر کاربر همان مرحله را خواسته باشد.

## صفحه اول پروژه

ریشهٔ ریپو یک `README.md` دارد (توضیح کلی + درخت اتصال **کل** پروژه).

بعد از هر اصلاح ساختار یا اتصال فایل‌ها، همان جلسه آن README را به‌روز کن.
