# دستورالعمل کار با ماژول ESP — ChangeOver

> راهنمای عملی بنچ برای اپراتور (مهندس برق). این سند جایگزین اسناد مرجع نیست؛ خلاصهٔ کاربردیِ آنهاست.
>
> تاریخ: 2026-09-23 | مبنا: فرم‌ور شاخهٔ `arena/01a0c744-changeover` (کامیت a8cdb1f) و پنل `esp_link_panel/esp_link_panel.ino`

**اسناد مرجع برای مطالعهٔ عمیق‌تر:**

| سند | چه چیزی در آن است |
|---|---|
| `ESP_AGENT_SPEC.md` | قرارداد کامل پروتکل v1.2 (جدول ۲۰ پارامتر، فریم‌ها، فرمول‌ها، مود دستی) |
| `Firmware/Modules/EspLink/README.md` | وضعیت ماژول + الزامات سمت ESP + دستورهای عامل ESP |
| `Circuit/ChangeOver(24V_DC).pdf` | شماتیک ۱۰ صفحه‌ای (ورودی/باتری/شارژر/MCU/هدر ESP) |
| `Firmware/Modules/EspLink/EspLink_Validation.xlsx` | تست‌های EI-001…EI-012 — نتیجهٔ کار بنچ را اینجا ثبت کنید |

---

## ۱. سیستم در یک نگاه

```
  ورودی 24V DC ──┐                          ┌── باتری 12V بالایی (Vhigh = V24 − V12)
                 ├─ [انتخاب/محافظت] ── STM32F103C8T6
  پک 24V (2×12V) ┘    │  │                    └── باتری 12V پایینی (Vlow = V12)
                      │  └─ USART1 → ESP8266-01 (هدر 2×4 روی برد) → وای‌فای → پنل مرورگر
                      └─ 2× شارژر flyback (Trans1/Trans2 + IRF640 + رلهٔ محافظ)
```

- **ESP = ماژول ESP8266-01** که روی هدر ۲×۴ خود برد می‌نشیند. پنل هم روی ESP8266 و هم ESP32 کامپایل می‌شود ولی سخت‌افزارِ این برد ESP8266-01 است.
- لینک: **USART1 با 921600 baud، 8N1** و DMA دوطرفه. PA9 (TX برد) → ESP_RX، PA10 (RX برد) ← ESP_TX، **PA8 = CH_PD** که فقط STM32 درایو می‌کند.
- **فرمان روشن‌شدن ESP فقط از میکرو می‌آید (بررسی‌شده 2026-09-23):** PA8 از همان ابتدای بوت به‌صورت خروجیِ Low درایو می‌شود (در `MX_GPIO_Init` اول لچ خروجی Low نوشته می‌شود و بعد پین خروجی می‌شود — بدون گلایچ) و **تنها مسیر High شدنش** در کل فرم‌ور `TaskComm → EspLink_Init → EspLink_Power(true)` است که فقط با `MODULE_ESP=1` اجرا می‌شود. سمت شماتیک هم پشتیبانی می‌کند: R36 (1k سری) بین PA8 و CH_PD و R37 (10k پول‌داون) روی خط CH_PD — یعنی اگر پین میکرو شناور بماند، ESP خاموش می‌ماند و روشن‌شدنش فقط با فرمان صریح میکرو ممکن است. (ریل 3V3 هدر همیشه وصل است؛ «روشن‌کردن» یعنی ENABLE از طریق CH_PD — مصرف ESP غیرفعال در حد میکروآمپر است.)
- برد بدون ESP هم کاملاً خودکار کار می‌کند (شارژر مستقل از پنل است). ESP فقط ابزار نظارت/کالیبراسیون/تست است.
- پنل مرورگر: AP به نام **«ChangeOver-ESP»** با رمز **«123456789»**، آدرس **http://192.168.4.1** — سه تب: **وضعیت / کالیبراسیون / تست دستی**.
- تله‌متری هر **۱۰۰ms** یک فریم است؛ اگر بیشتر از ~۱ ثانیه نیامد یعنی لینک پایین است (پنل هشدار می‌دهد، شارژر به کار خودش ادامه می‌دهد).

## ۲. قبل از اولین وصل‌کردن — چک‌لیست

1. **فرم‌ور STM32 هنوز ESP را روشن نکرده است:** `MODULE_ESP = 0` در `Firmware/Config/Inc/modules_enable.h` (سطر ۲۲) است. تا `1` نشود و برد فلش نشود، PA8 پایین می‌ماند و ESP اصلاً بوت نمی‌شود. این یک خط است و باید با rebuild کامل فلش شود.
2. **قطبیت و سطح منطقی:** همهٔ خط‌های هدر ESP منطق **3.3V** هستند. روی این هدر **ماژول سادهٔ ESP-01** بنشانید، نه برد توسعهٔ NodeMCU/Wemos — آن برد‌ها خودشان EN را پول‌آپ دارند و با رسیدن تغذیه بدون فرمان میکرو بوت می‌شوند (خلاف طراحی برد). ESP8266 **تحمل 5V ندارد** — هیچ تغذیه/سیگنال 5 ولتی (حتی از مبدل USB-TTL) به هیچ پین ESP وصل نکنید. روی برد مقاومت سری روی خط‌ها هست ولی به آن تکیه نکنید.
3. **زمین مشترک:** هر چیز دیگری که به لینک وصل می‌شود (USB-TTL برای دیباگ، مولتی‌متر سیگنالی و…) باید GND مشترک با برد داشته باشد. همیشه **اول GND**، بعد سیگنال.
4. **فلش ESP را جدا از برد انجام دهید** (روی بردبورد با آداپتور USB-TTL 3.3V)، بعد ماژول را روی هدر بنشانید. اگر می‌خواهید در‌جای فلش کنید، روی شماتیک (صفحهٔ ۴) خطوط `ESP_RST` و `ESP_GPIO0` از هدر به MCU می‌روند: GPIO0 را هنگام ریست Low نگه دارید تا بوت‌لودر باز شود — ولی برای بار اول روش جداگانه امن‌تر است.
5. **سیم‌های UART کوتاه** (< ۲۰ سانتی‌متر) و دور از ترانس/نود سوئیچینگ. 921600 روی سیم بلند/بی‌زمین خطا می‌دهد. در بدترین حالت پارسر برد روی `AA 55` دوباره سنکرون می‌شود و ربوت ESP8266 (که موقع بوت چند بایت آشغال می‌فرستد) مشکل‌ساز نیست.
6. **تغذیهٔ ESP:** طبق شماتیک برد خودش 3V3 می‌دهد. ESP8266 موقع TX وای‌فای پیک جریان قابل توجهی می‌کشد؛ اگر ریل 3V3 ضعیف باشد ESP ریبوت‌های تصادفی می‌گیرد (نشانه: پنل مدام می‌افت/می‌آید). اگر جدا تست می‌کنید تغذیهٔ 3.3V با توان کافی بدهید، **نه 5V**.
7. **زمین‌لوپ بنچ:** اگر برد را از منبع تغذیهٔ بنچ می‌گیرید و لپ‌تاپ (با شارژر) به پنل وصل است، مسیر زمین از طریق شارژرِ لپ‌تاپ بسته می‌شود. منبع بنچ را ایزوله/شناور کنید یا لپ‌تاپ را از برق بکشید.

## ۳. روشن‌کردن و اتصال به پنل

1. ترتیب امن روشن‌کردن: اول باتری‌ها/ورودی 24V به برد، بعد برد روشن. بوت ESP ممکن است ~۱۰ ثانیه طول بکشد تا AP بالا بیاید.
2. با گوشی/لپ‌تاپ به وای‌فای «ChangeOver-ESP» وصل شوید (رمز `123456789`) و در مرورگر `192.168.4.1` را باز کنید. پنل راست‌به‌چپ با فونت وزیرمتن است؛ اینترنت لازم نیست (AP خود ESP است).
3. **نشانهٔ سالم بودن لینک:** اعداد تب «وضعیت» هر لحظه تازه می‌شوند (فریم تله‌متری هر ۱۰۰ میلی‌ثانیه می‌رسد) و «seq» جلو می‌رود. اگر «ESP در دسترس نیست» دیدید یعنی مرورگر به AP وصل است ولی ESP لینک UART را نمی‌بیند → ماژول نشسته؟ `MODULE_ESP=1` فلش شده؟ GND؟ TX/RX جابه‌جا؟
4. دکمه‌های روشن/خاموش شارژر ۱ و ۲ (پارامتر ۱۱/۱۲) روی تب وضعیت هستند؛ خاموش‌کردن فوراً PWM را قطع می‌کند — این «کلید اضطراری نرم» شماست.

## ۴. کالیبراسیون — روش درست، گام‌به‌گام

پنل سه دستیار خودکار دارد (تب «کالیبراسیون») که همه‌چیز را از فرمول‌های واقعی فرم‌ور حساب می‌کنند. ترتیب زیر مهم است چون هر مرحله به مرحلهٔ قبلی تکیه می‌کند.

### زنجیرهٔ جریان (این را بفهمید، بعد کالیبره کنید)

هر کانال این زنجیره را طی می‌کند و پنل همهٔ مراحل را زنده نشان می‌دهد:

```
شنت 10mΩ → تقویت‌کنندهٔ LM358 (گین ×101) → مقسم 1k/10k → ADC 12-bit
raw counts → mA_خام = (raw − آفست) × 0.8776 × گین/1000   ← (پیش‌فرض: آفست=8، گین=1085‰ یعنی ≈×0.9523 mA/count)
→ فیلتر: اول median (پارامتر 7: پنجرهٔ 1/3/5)، بعد میانگین متحرک (پارامتر 8: پنجرهٔ 1..10)
→ iest (تخمین جریان سر باتری) = i_فیلترشده × Vin × η / Vbat
```

نکتهٔ کلیدی: **سنس جریان روی مسیر اولیهٔ ترانس (سمت 24V) است، نه سر باتری.** «جریان»ی که پنل نشان می‌دهد جریان اولیه است؛ جریانِ باتری فقط به‌صورت تخمین (`iest`) با بازدهی η محاسبه می‌شود.

### گام ۰ — آماده‌سازی

- مولتی‌متر اسیلوسکوپی لازم نیست؛ یک DMM خوب + یک آمپرمتر سری (یا DMM دوم روی رنج mA/ده میلی‌آمپر) کافی است.
- برد روشن، باتری‌ها وصل، ورودی 24V حاضر. وضعیت شارژر هرچه هست بگذارید باشد (برای گام ۱ بهتر است شارژر در حال کار باشد تا ولتاژها روی مقدار واقعی باشند).

### گام ۱ — کالیبراسیون ولتاژ (اول این)

چون فرمول `iest` از Vbat استفاده می‌کند، اول ولتاژها را درست کنید. سه نقطه در تب کالیبراسیون هست: **ولتاژ ورودی (پارامتر 4)، پک 24V (پارامتر 5)، نود 12V (پارامتر 6)**.

1. DMM را بین **ترمینال‌های روی خود برد** ببندید (همان نقاطی که برد سنس می‌کند — نه سر سیمِ باتری، تا افت سیم وارد خطا نشود): ورودی 24V نسبت به GND، پک 24V نسبت به GND، و نود میانی 12V نسبت به GND.
2. عدد DMM را (به ولت) در کادر دستیار همان نقطه وارد کنید و «اعمال» بزنید. آفست جدید خودکار حساب می‌شود: **آفست جدید = آفست قبلی + (واقعی − نمایش)** و در بازهٔ ±2000mV گیره می‌خورد.
3. بعد از اعمال، چک کنید عدد پنل به عدد DMM برسد (اختلاف چند ده میلی‌ولت طبیعی است).
4. یادتان باشد **Vhigh (باتری بالایی) = V24 − V12** محاسبه می‌شود؛ پس دقت هر دو آفست 5 و 6 در نتیجه اثر می‌گذارد.

### گام ۲ — صفرِ جریان (آفست، پارامتر 0/1)

1. جریان هر دو کانال را صفر کنید: در تب وضعیت هر دو شارژر را با دکمهٔ ON/OFF قطع کنید (یا duty=0 در مود دستی).
2. چند ثانیه صبر کنید تا نمونه‌ها جمع شود، بعد در تب کالیبراسیون دکمهٔ **«صفر = raw فعلی»** هر کانال را بزنید. دستیار میانگین ۱۰ نمونهٔ آخر raw را می‌نویسد و اگر duty صفر نباشد **اخطار می‌دهد** — آن اخطار را جدی بگیرید.
3. بعد از صفر، `ma_unfiltered` هر دو کانال باید ~۰ باشد (چند واحد نوسان طبیعی است).

### گام ۳ — گین جریان (پارامتر 2/3)

1. **آمپرمتر را سری با شنت اولیه ببندید — سمت ترانس، نه سر باتری** (همان مسیری که شنت 10mΩ روی آن است). جریان این آمپرمتر همان چیزی است که زنجیرهٔ سنس باید نشان دهد.
2. اجازه دهید شارژر در حالت BULK جریان بکشد (یا در مود تست دستی یک duty متوسط مثل 200‰ بدهید تا جریان پایدار شکل بگیرد).
3. عدد آمپرمتر را به **میلی‌آمپر** در کادر «گین» همان کانال بزنید. دستیار خودش حساب می‌کند: **گین جدید = گین فعلی × (آمپرمتر ÷ جریان فیلترشدهٔ پنل)** (گیره 100..3000‰).
4. اگر اختلاف زیاد بود (بیش از ~۱۰٪) بعد از اعمال دوباره چک کنید؛ آفست غلط در گام ۲ بزرگ‌ترین عامل خطاست.

### گام ۴ — فیلترها (پارامتر 7/8)

- نمودار ۳۶ ثانیه‌ای تب کالیبراسیون دو منحنی می‌کشد: خام (خاکستری) و فیلترشده (آبی) + «نوسان» هرکدام.
- پنجرهٔ بزرگ‌تر = عدد پایدارتر برای حلقهٔ تنظیم، ولی پاسخ کندتر. پیش‌فرض median=3 و average=10 برای شارژ معمولی خوب است؛ فقط اگر نوسان خام زیاد بود (محیط نویزی) سراغ median=5 بروید. **پنجرهٔ 1 = خاموش‌کردن آن مرحله فیلتر** (برای دیدن اثر فیلتر، موقتاً 1/1 بگذارید و مقایسه کنید).
- بعد از هر تغییر فیلتر، حالت فیلتر ریست می‌شود — چند ثانیه صبر کنید تا نمودار معنادار شود.

### گام ۵ — صحه‌گذاری تخمین (η، پارامتر 9/10)

- η **فقط روی عدد تخمینی `iest` اثر دارد، نه روی شارژ واقعی.** پس این گام خطری ندارد ولی حتماً انجامش دهید چون تصمیم‌های شما (مثلاً «چقدر شارژ وارد شده») روی همین عدد است.
- راه درست: آمپرمتر دوم را این‌بار **سری با باتری** (سمت ثانویه) ببندید و با `iest` پنل مقایسه کنید. اختلاف را با η تنظیم کنید: η_جدید = η_فعلی × (واقعی ÷ iest). دامنهٔ مجاز 100..999‰.
- ⚠️ **مقدار η کانال ۲ (پیش‌فرض 242) عمداً غیرفیزیکی است** — خطای over-read سنس کانال ۲ را جذب می‌کند. آن را «اصلاح» نکنید و به ~700 نبرید مگر کل زنجیرهٔ سنس (صفر+گین) را از نو کالیبره کرده باشید و خطا واقعاً رفع شده باشد.
- **مغایرت بازِ شناخته‌شده (نقطهٔ بنچ 2026-09-22):** تخمین ~۳۶۴mA در برابر ~۵۰۰mA واقعی — هنوز تطابق کامل نیست. تا وقتی با آمپرمتر سر باتری صحه‌گذاری نکرده‌اید، به `iest` برای تصمیم‌های کمی اتکا نکنید.

### گام ۶ — ثبت نتیجه‌ها (مهم!)

- **همهٔ پارامترها فقط در RAM برد هستند:** با هر ریست STM32 همه‌چیز به پیش‌فرض‌های کامپایل‌شده برمی‌گردد. پنل مقادیری را که *در همین نشست* داده‌اید بعد از ریستِ تشخیص‌داده‌شدهٔ برد دوباره می‌فرستد (مود دستی هرگز خودکار روشن نمی‌شود)؛ ولی اگر خود ESP ریبوت شود حافظهٔ نشست می‌رود.
- پس: **اعداد نهایی کالیبراسیون را روی کاغذ/فایل بنویسید** و نتیجهٔ هر تست را در `EspLink_Validation.xlsx` ثبت کنید. مقادیر نهاییِ تأییدشده سرانجام باید در فرم‌ور کامپایل شوند تا پیش‌فرض واقعی شوند.
- بعد از هر SET_PARAM، پاسخ `PARAM_REPORT` **مقدار اعمال‌شده** را برمی‌گرداند که ممکن است با درخواست شما فرق کند (گیرهٔ دامنه) — پنل همین عدد اعمال‌شده را نشان می‌دهد؛ اگر با درخواست فرق داشت یعنی خارج از بازه بوده.

## ۵. مود تست دستی (تب سوم) — ابزار کالیبراسیون و بنچ

این مود (پارامتر 19) **کل شارژر خودکار را خاموش می‌کند و duty هر کانال را خودتان مستقیم می‌گذارید.** برای دیدن زنجیرهٔ جریان با duty ثابت و تنظیم آفست/گین/فیلتر ساخته شده — با یا بدون باتری.

**چه چیزی خاموش می‌شود:** کل ماشین حالت (BULK/ABSORB/FLOAT، رمپ‌ها، ریتری JIT، ۱۵ ثانیه انتظار پایداری باتری)، پنجرهٔ اعتبار باتری، هشدار BAT_LOST، و توقفِ ۱۴٫۴V.

**چه چیزی روشن می‌ماند (کف سخت‌افزاری — قابل دور زدن نیست):**
1. حضور ورودی 24V (زیر 22V → duty صفر، وضعیت INPUT_WAIT؛ برگشت ورودی → duty فرمان‌شده فوراً برمی‌گردد)
2. تریپ سخت‌افزاری JIT (کامپراتور LM393، محافظ اشباع ترانس) — در مود دستی **ریتری خودکار ندارد**: دکمهٔ «اعمال مجدد duty» کانال را مسلح می‌کند. **سومین تریپ = FINAL_FAULT که فقط با ری‌استارت برد پاک می‌شود.**
3. قطع سخت ۱۵٫۰V هر کانال (محافظت استیج خروجی وقتی باتری ولتاژ را مهار نمی‌کند)
4. سقف duty (کمینهٔ سقف ۵۰۰‰ ثابت و سقف پارامتری 13/14)
5. قطع کانال 11/12

**قواعد استفاده:**
- روشن‌کردنش پشت دیالوگ تأیید است («شارژر خودکار و همهٔ محافظت‌های باتری متوقف می‌شوند...») — درست است، دقیقاً همین اتفاق می‌افتد.
- duty را با اسلایدر/عدد (0..500‰) بدهید؛ **فوراً و بدون رمپ** اعمال می‌شود.
- **ددمن لینک ۳ ثانیه است:** تا وقتی مود دستی روشن است پنل باید هر ۱ ثانیه یک فریم بفرستد (خود ESP این کار را می‌کند، از هر تب و در پس‌زمینه). نشانگر «پایش لینک فعال / keepalive» را زیر نظر داشته باشید. اگر لینک ۳ ثانیه ساکت بماند، برد هر دو duty را صفر می‌کند، از مود دستی خارج می‌شود و **شارژر خودکار از نو شروع می‌کند** (با ۱۵ ثانیه انتظار).
- پنل بسته/فریزشده (مثلاً خاموش‌شدن صفحهٔ گوشی): بعد از ۱۰ ثانیه خود ESP فرمان خروج (19=0) را می‌فرستد. یعنی مود دستی هرگز بی‌ناظر نمی‌ماند — ولی در همان پنجرهٔ ~۱۰ ثانیه، duty شما بدون ناظر اجرا می‌شود؛ **پس موقع کار دستی، صفحه را روشن و تب را باز نگه دارید.**
- دکمهٔ **ALL-OFF** هر دو duty را صفر می‌کند — کلید امن شما وسط کار.
- موقعیت‌های وضعیت که ممکن است ببینید: `9=MANUAL`، `5=JIT_RETRY_WAIT` (بعد تریپ؛ «اعمال مجدد duty»)، `6=INPUT_WAIT`، `7=FINAL_FAULT` (فقط ریبوت).
- **مود duty فیکس (پارامتر 15..18) چیز دیگری است:** آن یکی داخل مسیر تنظیم می‌ماند و بالای ولتاژ ابزورب خودش قطع می‌شود. مود دستی همین قیدها را هم ندارد. این دو را با هم اشتباه نگیرید.
- **خروج از مود دستی = باتری‌ها دوباره شارژ خودکار می‌شوند.** اگر می‌خواهید همه‌چیز خاموش بماند، اول شارژرها را با 11/12 قطع کنید بعد از مود دستی خارج شوید.

## ۶. خطرات و نکات ایمنی

### خطرات اصلی (به ترتیب جدیت)

1. **اتصال کوتاه باتری سربی.** دو باتری 12V سری = پک 24V با جریان اتصال کوتاه صدها آمپر. انگشتر/ساعت/ابزار فلزی دور ترمینال‌ها نیندازید. در مسیر هر باتری **فیوز سری** بگذارید (چند آمپر کافی است) قبل از هر کار بنچ. قطبیت را دوبار چک کنید — شماتیک برای پک 24V سری است.
2. **جرقهٔ اتصال خازن‌ها.** خازن‌های 220µF/50V ورودی شارژر موقع وصل باتری/ورودی جرقه می‌زنند — طبیعی است، ولی ابزار را جلوی ترمینال نگه ندارید.
3. **نود سوئیچینگ flyback.** MOSFETهای IRF640 (روی 200V ریتینگ) و ترانس‌های EE33 در حال سوئیچ با فرکانس بالا؛ نود درین اسپایک ولتاژی دارد. با اسکوپ فقط بعد از اطمینان از زمین ایزوله بنچ کار کنید و پروب را روی درین نگذارید مگر رنج و نوک مناسب داشته باشید. حرارت این قطعات در duty بالا جدی است — تست لمسی/دورسنجی کنید.
4. **مود دستی بدون باتری = حلقهٔ بسته وجود ندارد.** با بار مقاومتی/مصرفی کار کنید، نه مدار باز؛ تنها سدِ اضافه‌ولتاژ، قطع سخت ۱۵٫۰V است. اگر باتری وصل است و ۱۵V قطع می‌کند، یعنی duty بیش از اندازه بالا بوده — پله‌پله بالا بروید.
5. **تریپ‌های JIT پشت‌سرهم.** هر تریپ یعنی ترانس به اشباع رفته (duty زیاد برای آن ولتاژ). مسلح‌کردن مکرر بدون فکر کردن، ترانس و MOSFET را گرم و در نهایت سومین تریپ = FINAL_FAULT (رلهٔ فیزیکی باز می‌شود و تا ریبوت برد هیچ درایوی ممکن نیست). جای کلید/طریقه قطع تغذیهٔ برد را قبل از شروع بدانید.
6. **ورودی 24V.** روی خود برد برق شهری نیست ولی اگر ورودی از هر منبعی می‌آید، ماهیت ایزولاسیون آن منبع را بدانید (منبع بنچ شناور/ایزوله بهترین انتخاب است؛ از خروجی رکتیفایر مستقیمِ ترانس شبکه‌ای روی بنچ استفاده نکنید مگر با ترانس ایزوله).
7. **5V = مرگ ESP8266.** هیچ پین ESP تحمل 5V ندارد. آداپتور USB-TTL فقط در مود 3.3V و فقط برای فلش جداگانه.
8. **امنیت وای‌فای.** AP بازِ بنچ با رمز پیش‌فرض `123456789` — **هرکس در برد وای‌فای باشد می‌تواند شارژر را کنترل کند.** برای هر استفاده‌ای فراتر از میز بنچ، `ESP_WIFI_AP_SSID`/`ESP_WIFI_AP_PASS` را در `esp_link_panel.ino` عوض کنید و دوباره فلش کنید.
9. **اعتماد به اعداد بدون کالیبراسیون.** تا کالیبراسیون گام ۱-۵ انجام و ثبت نشده، اعداد جریان/ولتاژ پنل «تقریبی» هستند — تصمیم‌های کمی (قطع/ادامه شارژ، قضاوت دربارهٔ سلامت باتری) را فقط با DMM بگیرید.
10. **گاز باتری سربی.** در ولتاژ ابزورب (۱۴٫۴V به بالا) باتری سربی-اسیدی گاز هیدروژن می‌دهد؛ تهویه داشته باشید و قطرات اسیدی روی ترمینال‌ها را تمیز کنید. (ولتاژ ست‌پوینت‌ها از ESP قابل تغییر نیست — عمداً.)

### اشتباه‌های رایج

| اشتباه | نتیجه | پیشگیری |
|---|---|---|
| hot-plug کردن ESP روی هدر وقتی برد روشن است | اسپایک/اتصال لحظه‌ای پین‌ها | برد را خاموش کنید، ماژول را بنشانید، روشن کنید |
| فلش ESP در جا با اتصال USB-TTL به TX/RX | دو درایور روی یک خط | فلش را جدا از برد انجام دهید |
| اتکا به ددمن به‌عنوان استاپ اضطراری | ددمن به شارژر *خودکار* برمی‌گردد، خاموش نمی‌کند | استاپ نرم = دکمه‌های ON/OFF شارژر (11/12)؛ استاپ واقعی = فیوز/کلید تغذیه |
| تغییر η کانال ۲ به مقدار «فیزیکی» ~700 | تخمین iest2 چند برابر اشتباه می‌شود | 242 را حفظ کنید مگر بعد از کالیبراسیون کامل زنجیره |
| کالیبراسیون ولتاژ با DMM سر سیم باتری (نه ترمینال برد) | آفست، افتِ سیم را هم جذب می‌کند | DMM روی ترمینال‌های برد |
| رها کردن گوشی با صفحهٔ خاموش در مود دستی | بعد از ~۱۰ ثانیه مود بسته می‌شود و شارژ خودکار برمی‌گردد | صفحه را روشن نگه دارید یا از لپ‌تاپ استفاده کنید |
| فرستادن انبوه فریم (ابزار/اسکریپت شخصی) | سرریز بافر RX برد (۲۵۶ بایت، تخلیه هر ۱۰۰ms) | انفجارهای ESP→برد را زیر ~۲۵۰ بایت نگه دارید |

## ۷. عیب‌یابی سریع

| نشانه | محتمل‌ترین علت | کار |
|---|---|---|
| AP هست، پنل «ESP در دسترس نیست» | لینک UART پایین است | نشستن ماژول، `MODULE_ESP=1`، GND، جابه‌جایی TX/RX |
| اصلاً AP بالا نمی‌آید | تغذیه/بوت ESP یا PA8 پایین | 3V3 هدر را ببینید؛ CH_PD باید High باشد (وقتی TaskComm اجراست) |
| اعداد ثابت می‌مانند > 1s | لینک پایین؛ پنل خاکستری نشان می‌دهد | شارژر خودش ادامه می‌دهد — از خود برد خبر بگیرید (LED/رله) |
| مقدار اعمال‌شده با درخواست فرق دارد | گیرهٔ دامنهٔ پارامتر | بازهٔ مجاز همان پارامتر را در جدول ببیند (ESP_AGENT_SPEC §5) |
| وضعیت 5 (JIT_RETRY_WAIT) | تریپ اشباع ترانس | duty را کم کنید و «اعمال مجدد duty» بزنید |
| وضعیت 7 (FINAL_FAULT) | سومین تریپ JIT یا خطای جریان | فقط ری‌استارت برد؛ قبل از ریست علت را پیدا کنید |
| وضعیت 6 (INPUT_WAIT) | ورودی < 22V | منبع 24V را چک کنید |
| بعد از ریست برد همهٔ تنظیمات برگشت | پارامترها RAM-only | از یادداشت خودتان دوباره اعمال کنید (نشست پنل هم کمک می‌کند) |
| پنل مدتی عدد کهنه نشان می‌دهد بعد درست شد | پنجرهٔ wrap شمارندهٔ seq (هر ~۱۰۹ دقیقه) | نوسازی خودکار هر ۳۰ ثانیه درستش می‌کند — نگران نباشید |
| keepalive متوقف است | تب فریز/مرورگر خواب | تب را فعال کنید؛ در بدترین حالت بعد ۱۰s مود دستی بسته می‌شود |
| با روشن‌شدن کانال دوم، اعداد کانال اول (یا هر دو) شیفت/نویز می‌گیرد | کراس‌تاک الکتریکی دو استیج flyback (ممیزی مو‌به‌مو 2026-09-23: زنجیرهٔ نرم‌افزار ADC→متغیرها per-channel سالم است) | تست A/B دوکانالهٔ زیر را اجرا کنید و امضا را بخوانید |
| raw یک کانال بین ~۵۰ (مگنتی‌زاسیون) و ~۸–۲۰ می‌پرد | کانال **بی‌بار** است: خروجی تا ۱۵٫۰V پمپ می‌شود → کف سخت duty=0 → افت → روشن (نوسان relaxation، بدون هیسترزیس)؛ با پارک‌شدن گیت، نمونهٔ سنکرون هم نمی‌آید و raw به مقدار async می‌افتد (یافتهٔ بنچ 2026-09-23) | باتری/بار آن کانال را وصل کنید یا duty اش را صفر کنید؛ روی پنل ببینید ولتاژ کانال نزدیک 15.0V می‌چرخد |
| کانال کاملاً پارک (duty=0) ولی raw آن با روشن‌بودن همسایه بالا می‌رود (بنچ 2026-09-24: raw2≈۲۰۰ با شارژر۱ روی ۲۰٪، در حالی که خاموش‌کردن شارژر۲ → raw1=۸ تمیز) | **کوپلینگ آنالوگ سخت‌افزاری یک‌طرفه** — سیگنال روی خود پایهٔ ADC است، قبل از MCU؛ زنجیرهٔ اندازه‌گیری فریم‌ور تبرئه شد. ~۲۰۰ count ≈ ~۱۷mV در ورودی LM358 کانال؛ شاهد هم‌راستا: افت ~0.2V خوانش باتری همسایه هنگام شارژ کانال دیگر (IR مسیر مشترک نود میانی/OUT_12) | اسکوپ AC روی خروجی LM358 کانال قربانی یا پایهٔ PA؛ بای‌پس U5 (C56/C57) سر جایش باشد؛ سنس Kelvin شنت (R64/R68)؛ جداسازی مسیر برگشت OUT_12 |
| بودج سیم زمینِ شنت → ستارهٔ ورودی، خطا را کم کرد ولی صفر نه (بنچ 2026-09-24) | **تأیید مکانیزم گالوانیک**: جریان برگشتی کانال ۱ روی مس زمین مشترک با مرجع سنس کانال ۲ افت می‌اندازد؛ باقی‌مانده = امپدانس خود سیم نازک (ده‌ها mΩ + سلف) + سهم القایی حلقهٔ سنس | سیم را کوتاه و ضخیم کن (یا ۲-۳ سیم موازی تابیده) تا منفی خازن‌های بزرگ ورودی؛ اگر باز ماند: جفت‌سیم تابیده از دو سر شنت (R68) تا ورودی آمپ؛ هدف raw کانال پارک ≤ ~۱۵-۲۰؛ در پایان کالیبراسیون با هر دو کانال روشن |
| اتصال زمین U5 (LM358) → زمین شنت: نویز به‌شدت کم شد (بنچ 2026-09-24) | **مرجع‌گذاری کلوین**: مرجع اندازه‌گیری آمپ به نقطهٔ زمین خود شنت منتقل شد → افتِ مس مشترک دیگر وارد اندازه‌گیری نمی‌شود. این سیم «سیم سنس» است (فقط µA می‌کشد)، نه مسیر پاور → حلقهٔ زمین خطرناک نیست | نگه دارید (لحیم محکم، کوتاه؛ نازک بودنش اشکال ندارد)؛ عدد متریک را ثبت کنید؛ چون زمین یک چیپ مشترک است، آفست هر دو کانال ممکن است جابه‌جا شده باشد → کالیبراسیون نهایی با هر دو شارژر روشن |
| آزمایش هم‌فازکردن گیت‌ها (2026-09-24): `BSP_PWM_TIM3_PHASE_OFFSET_IN_PHASE=1` | دستور کاربر برای بررسی سهم فاز در کراس‌تاک باقی‌مانده. **پیش‌بینیِ از پیش ثبت‌شده:** تغییر عددی نباید زیاد باشد — چون (۱) با پارک‌بودن کانال ۲، فاز TIM3 اصلاً اهمیتی ندارد (کانال ۲ سوییچ نمی‌کند؛ آرتیفکت را جریان کانال ۱ می‌سازد)، (۲) خوانش‌ها با فیلتر آنالوگ 470nF میانگین‌اند و به فاز کورند. آنچه عوض می‌شود: شکل موج اسکوپ (لگدها روی هم می‌افتند) و ریپل ورودی (دوبرابر وقتی هر دو روشن‌اند) | متریک‌ها را با حالت interleave مقایسه کنید: raw2 پارک با ch1@۲۰٪ (مرجع: ~۲۰)، هر دو @۱۰٪ (مرجع: 101/242/450)، شکل موج U5B. بعد از آزمایش مقدار را به 0 (تولید) برگردانید مگر داده چیز دیگری بگوید. duty را در حالت هم‌فاز ≤ ۲۰٪ نگه دارید |
| حالت دوکاناله: raw یک کانال ~نصف می‌شود ولی جریان واقعی (مولتی‌متر) و گیت (اسکوپ) ثابت‌اند؛ JIT هم نیست (بنچ 2026-09-24) | **خطای مرجع ×۱۰۱**: آمپ غیروارونده است، پس هر اختلاف «زمین شنت» با «زمین مرجع مشترک آمپ» ×۱۰۱ وارد خوانش می‌شود. بودج زمین LM358→زمین شنتِ فقط یکی، آن کانال را بی‌خطا و کانال دیگر را در کار هم‌زمان خراب می‌کند (~۱mV کافی است). هم‌فاز/interleave تفاوتی نکرد (اثر DC است) | سیم مرجع را به **ستارهٔ زمین** (منفی خازن‌های بزرگ ورودی) ببرید و دوباره کالیبره کنید؛ اگر ناکافی ماند: Rg هر آمپ را جدا به زمین شنت خودش؛ راه‌حل تولید در PCB بعدی = آمپ سنس جریان تفاضلی واقعی per-channel (مثل INAxxx) |


**نتیجهٔ آزمایش هم‌فاز (2026-09-24): بدون تغییر قابل‌اندازه‌گیری — پیش‌بینی درست بود (فیلتر آنالوگ + پارک‌بودن کانال دوم یعنی فاز بی‌اثر). کلید به `0u` (درهم‌گذاری تولید) برگشت؛ ریفلش لازم است.**

### تست A/B دوکاناله — جدا کردن علت در ۱۰ دقیقه (2026-09-23)

پس‌زمینه: دو گیت با درهم‌گذاری ثابت ۱۰µs کار می‌کنند؛ لحظهٔ نمونه‌برداری سنکرون هر کانال (وسط ON خودش) به‌طور معمول داخل پنجرهٔ **تخلیهٔ ترانس کانال دیگر** می‌افتد. جریان ثانویهٔ کانال دیگر از نود باتری میانی، زمین‌های مشترک، ریل ۲۴V مشترک و حتى پکیج مشترک LM358 عبور می‌کند — پس وقتی هر دو کانال در مدار باشند یک خطای سیستماتیک وابسته به بار کانال دیگر روی هر دو خوانش نشست می‌گیرد. این پدیده سخت‌افزاری است؛ ممیزی 2026-09-23 کل زنجیرهٔ نرم‌افزار (رتبه‌های اسکن ADC، بافر DMA، splice سنکرون، فیلترها، متغیرها، پارامترهای ESP، بسته‌بندی تله‌متری) را per-channel سالم اعلام کرد.

1. تب «تست دستی» → فیلترها را موقتاً خاموش کنید (مدین=۱، میانگین=۱) تا خام ببینید.
2. **A:** فقط کانال ۱ با duty متوسط (مثلاً ۲۵۰‰) — خاموش کانال ۲. عدد raw1/ma1 را یادداشت کنید.
3. **B:** حالا کانال ۲ را با همان duty روشن کنید و فقط raw1 را تماشا کنید (چند ثانیه). بعد برعکسش را تکرار کنید (اثر کانال ۱ روی raw2).
4. امضا را بخوانید:
   - **شیفت ثابت (DC) به‌اندازهٔ معین که با duty کانال دیگر مقیاس می‌شود** = کراس‌تاک الکتریکی تأیید شد → راه‌حل: کالیبراسیون گین/η را *با هر دو کانال روشن در بار معمول* انجام دهید (دستیارها همین را می‌کنند) تا خطای سیستماتیک جذب شود؛ برای ریشه‌ای‌تر: مسیر Kelvin شنت‌ها، سیم‌کشی نود میانی و زمین مشترک را بازبینی کنید.
   - **کانال پارک + همسایهٔ روشن → raw بالای آفست (~۲۰۰)** = کوپلینگ آنالوگ سخت‌افزاری یک‌طرفه (تأیید 2026-09-24) → سخت‌افزار: اسکوپ خروجی LM358، بای‌پس، Kelvin، مسیر OUT_12؛ موقتاً: کالیبراسیون با هر دو کانال روشن.
   - **پرش raw بین ~۵۰ و ~۸** = کانال بی‌بار (پمپ ۱۵٫۰V + قطع/وصل، یافتهٔ 2026-09-23) → باتری وصل کنید یا duty صفر؛ در تست A/B **هر دو کانال باید بار دار باشند** وگرنه آلوده‌گر همان کانال بی‌بار است.
   - **نویز تصادفی/جهش بین دو سطح دیگر** = مسیر دیگری است → گزارش دهید تا عمیق‌تر برویم.
   - **هیچ تغییری در A/B** = مشکل شما جای دیگری است (مثلاً فقط Vhigh/Vlow/iest خراب‌اند؟) → همان موقع نود میانی/باتری‌ها را با اسکوپ ببینید؛ Vhigh = V24−V12 از دو نود مشترک ساخته می‌شود و ریپل تخلیهٔ هر دو کانال را می‌بیند.
5. عکس/عدد تست را در `EspLink_Validation.xlsx` (تست‌های EI) ثبت کنید.

## ۸. قوانین طلایی (خلاصهٔ خلاصه)

1. زمین مشترک، منطق 3.3V، سیم UART کوتاه — و ESP8266 هرگز 5V نمی‌بیند.
2. CH_PD مال برد است؛ از سمت ESP به آن دست نزنید.
3. تا `MODULE_ESP=1` فلش نشود، هیچ‌چیز روشن نمی‌شود.
4. کالیبراسیون به ترتیب: **ولتاژ → صفر → گین (آمپرمتر سمت ترانس) → فیلتر → η** — و همه را بنویسید، چون با ریست برد می‌پرند.
5. η کانال ۲ = 242 عمدی است؛ دست نزنید.
6. مود دستی: کف سخت‌افزاری (ورودی/JIT/15V/سقف) باقی است ولی **همهٔ محافظت‌های باتری خاموش‌اند** — با DMT کنار باشید، صفحه روشن، ALL-OFF در دسترس.
7. خروج از مود دستی یعنی برگشت شارژ خودکار — برای خاموش‌ماندن، اول 11/12 را قطع کنید.
8. استاپ اضطراری واقعی شما فیوز/کلید تغذیه است، نه نرم‌افزار.
9. AP باز است؛ برای فراتر از بنچ رمز را عوض کنید.
10. تصمیم کمی فقط با DMM؛ پنل تا پایان کالیبراسیون «تقریبی» است.

## ۹. چک‌لیست چیدمان PCB نسخهٔ بعد — ریشه‌کن‌کردن کراس‌تاک کانال‌ها
_(درس‌آموختهٔ پروندهٔ ۲۰۲۶-۰۹-۲۲ تا ۲۰۲۶-۰۹-۲۴؛ شواهد: کانال پارک با همسایهٔ روشن raw≈۲۰۰ می‌خواند، بودج زمین LM358→شنت آن را تقریباً صفر کرد، افت 0.2V خوانش باتری همسایه با شارژ کانال دیگر، آزمایش فاز بی‌اثر)_

1. **زمین ستاره‌ای در منفی خازن‌های ورودی:** جریان برگشتی قدرت هر کانال (پایهٔ زمین شنت → منفی خازن‌های ورودی) با مس مستقیم و ضخیم و **جدا از کانال دیگر**. هیچ مس قدرتی نباید بین نقطهٔ مرجع سنس یک کانال و شنتش رد شود.
2. **مرجع‌گذاری کلوین هر آمپ:** پایهٔ ۴ U5 و زمینِ مقسم‌های ورودی هر نیمهٔ LM358، مستقیم به نود زمین **شنت همان کانال** (کشفِ بودج ۲۰۲۶-۰۹-۲۴ — این بار به‌صورت طراحی و برای هر کانال جدا). سیم مرجع سنس است و فقط µA می‌کشد؛ نازک بودن مشکلی ندارد، کوتاه بودن مهم است.
3. **خطوط سنس جفتی و کوتاه:** دو خط ShuntX تا U5 موازی و چسبیده به هم (حداقل سطح حلقه)، دور از حلقهٔ قدرت (درین ماسفت → ترانس → خازن ورودی) هر دو کانال.
4. **تپ جدا برای سنس V12:** تقسیم‌کنندهٔ ولتاژ نود میانی از خود ترمینال باتری تپ بخورد، نه از مس جریان‌دار OUT_12 (اصلاح خطای 0.2V). برای V24/Vlow هم اگر مسیر جریان‌دار است همین‌طور.
5. **بای‌پس U5:** خازن‌های C56/C57 چسبیده به پایه‌های ۸/۴ خود چیپ.
6. **درهم‌گذاری ۱۰µs گیت‌ها حفظ شود** (آزمایش نشان داد بر خطا اثر ندارد، ولی برای ریپل ورودی و EMC مفید است).
7. **معیار پذیرش برد جدید (همه باید پاس شوند):**
   - کانال پارک + همسایه @۲۰٪ → raw کانال پارک ≤ ~۱۵
   - خاموش‌کردن/روشن‌کردن هر کانال → تغییر خوانش ولتاژ باتری کانال دیگر ≤ ~۵۰mV
   - raw هر کانال با duty² خطی (تست در ۵٪/۱۰٪/۲۰٪)
   - اعداد با آمپرمتر منبع (منهای ~۹mA خود برد) همخوان پس از کالیبراسیون
