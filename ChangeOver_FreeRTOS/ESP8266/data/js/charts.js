async function getJson(url){const r=await fetch(url);if(!r.ok)throw new Error(r.status);return r.json()}
function draw(canvas,items,a,b,colors){const c=canvas.getContext('2d'),w=canvas.width,h=canvas.height;c.clearRect(0,0,w,h);c.strokeStyle='#2b3a4a';c.strokeRect(0,0,w,h);if(!items.length)return;let max=1;items.forEach(x=>{max=Math.max(max,Number(x[a]),Number(x[b]))});colors.forEach((color,idx)=>{c.strokeStyle=color;c.beginPath();items.forEach((x,i)=>{const px=20+(w-40)*i/Math.max(1,items.length-1),py=h-20-(h-40)*Number(x[idx?b:a])/max;if(i===0)c.moveTo(px,py);else c.lineTo(px,py)});c.stroke()})}
async function refresh(){try{const d=await getJson('/api/telemetry');const items=d.records||[];draw(document.getElementById('voltageChart'),items,'input24vMv','battery24vMv',['#55aaff','#39d98a']);draw(document.getElementById('currentChart'),items,'current1Ma','current2Ma',['#f4c95d','#ff6b6b']);document.getElementById('chartStatus').textContent='Samples: '+items.length}catch(e){document.getElementById('chartStatus').textContent='Charts disabled or API error: '+e}}
refresh();setInterval(refresh,5000);
